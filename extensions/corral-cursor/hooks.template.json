{
  "//": "Reference only. The extension writes the real ~/.cursor/hooks.json on activation (merging additively, never clobbering existing hooks), substituting <NODE> with node's absolute path and <ABSOLUTE> with its own extensionPath/state-hook.js. Cursor's schema: each stage is a flat array of { command: \"<one whitespace-tokenized string>\" } (no type/args/nested hooks).",
  "version": 1,
  "hooks": {
    "beforeSubmitPrompt": [
      { "command": "<NODE> <ABSOLUTE>/state-hook.js beforeSubmitPrompt" }
    ],
    "stop": [
      { "command": "<NODE> <ABSOLUTE>/state-hook.js stop" }
    ]
  }
}
